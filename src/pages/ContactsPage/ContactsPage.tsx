import './ContactsPage.scss';

type Props = {};

const ContactsPage: React.FC<Props> = ({}) => {
  return (
    <section className='ContactsPage'>
      <h1>Contacts Page Title</h1>
    </section>
  );
};

export default ContactsPage;

