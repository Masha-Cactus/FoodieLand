import './NotFoundPage.scss';

type Props = {};

const NotFoundPage: React.FC<Props> = ({}) => {
  return (
    <section className='NotFoundPage'>
      <h1>Not Found Page Title</h1>
    </section>
  );
};

export default NotFoundPage;

