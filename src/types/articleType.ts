export interface BlogType {
  title: string;
  description: string;
  date: string;
  author: string;
}

export interface ArticleType {
  title: string;
  description: string;
  mainText: string;
  date: string;
  author: string;
  imgUrl: string;
}
